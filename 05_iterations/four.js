const myobject ={
    js:'javascript',
    cpp:'c++',
    rb:"ruby"
}

for (const i in myobject) {
    // if (Object.hasOwnProperty.call(object, key)) {
    //     const element = object[key];
        
    // }
//    console.log(`${i}  is a key of ${myobject[i]}`)
}


//array ke case me key print hoga automatically

//map iterable nahi hai ye for in loop se nahi hoga

